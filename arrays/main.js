/* let nombre = "harold"
let edad = 26

// arrays


let frutas = ["fresa", "mango", "platano"];
console.log(frutas[1]);

// objetos

let persona = {
    nombre: "harold",
    edad: 26,
};

console.log(persona.edad); */


// array
/* 
let marcas = ["bmw", "toyota", "nissan"]
console.log("la marca de auto es" + marcas[1]); */

/* let personas=["kevin", "emir", "ana"]
let notas = [100, 70, 40, 80]
let cantidad = personas.length

let resultado= 0;
let cantidadExamenes= notas.length
for (let index = 0; index < notas.length; index++) {
    resultado = resultado+ notas[index];
}
console.log(resultado/cantidadExamenes)

 */

/* let numero = [1, 2, 3, 4];
for (let index = 0; index < numero.length; index++) {
    numero[index] = numero[index] * 2;
}
console.log(numero); */
/* 
let numeros = [1, 2, 3]

for (let index = 0; index < numeros.length; index++) {
    res = 1
    for (let j = 1; j <= numeros[index]; j++) {
        res = res * j
    }

    numeros[index] = res

    console.log(numeros);
} */


/* let matriz = [
    [1, 2, 3],
    [3, 4, 3],
    [2, 4, 5],
]
let resultado = 0
for (let index = 0; index < matriz.length; index++) {
    resultado = resultado + matriz[index][index]
}
console.log(resultado) */

/* let numeros = [2, 4, 8, 10];

let numeroMitad = numeros.map((numeros) => numeros / 2)
console.log(numeroMitad) */

/* let numeros = [3, 11, 15, 4, 2, 17, 8, 10, 22, 7]

let numerosPares = numeros.filter((x) => x % 2 === 0);
console.log(numerosPares) */

